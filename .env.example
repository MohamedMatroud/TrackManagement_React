# Browser requests use this path. Vite proxies it during local development.
VITE_API_BASE_URL=/api

# Local .NET API target used only by the Vite development server.
API_PROXY_TARGET=https://localhost:7049
